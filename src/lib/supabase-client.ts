'use client';

// TODO PORTAL: Browser-side Supabase client for auth + realtime.
//
// Shape (@supabase/ssr):
//   import { createBrowserClient } from '@supabase/ssr';
//   export const supabase = createBrowserClient(
//     process.env.NEXT_PUBLIC_SUPABASE_URL!,
//     process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
//   );
//
// Use in client components for:
// - signInWithPassword, signInWithOAuth (Google), signUp, signOut
// - resetPasswordForEmail
// - onAuthStateChange listener (e.g. for "you've been logged out elsewhere")

export {};  // placeholder

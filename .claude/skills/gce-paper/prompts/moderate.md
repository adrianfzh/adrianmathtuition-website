You are the SEAB moderator for slot(s) __SLOTS__ of a new GCE O-Level __SUBJECT__ (__CODE__) Paper __P__. For each slot:
1. Read `__RUN__/Q<n>.moderate.md` — it holds your full brief (the two jobs: CHECK THE KEY against the blind examiner's `Q<n>.blind.json`, and JUDGE THE QUESTION), the question, the setter's key and worked solution, and the exemplars the setter was shown. Follow it exactly and write `__RUN__/Q<n>.verdict.json` in exactly the JSON shape it specifies.
2. ALSO read `__RUN__/standard.md` and `__RUN__/standard-questions-P__P__.md` once. The tutor who commissions these papers rejected the previous draft as "too easy": the standard is the 2024/2025 papers. Apply the moderator's test in standard.md. A question BELOW the 2024/25 standard for its marks scores at most 3 (the assembly step then rejects it) and your "fixes" must say concretely what would bring it to standard (which scaffold to remove, which decision to hand to the candidate, which two ideas to chain). A question ABOVE the standard (unfair for the marks or the time) also scores at most 3, with fixes. ROUTINE SLOTS: where standard.md defines a ROUTINE slot (the A Math standard does, §4 List A and §5), a clean routine question of a kind 2024/25 kept is NOT below standard: check it against the short list standard.md gives for a routine slot, score it 4 when it is clean, and write "standard": "routine". You judge the question only; the session counts routine slots against the paper's allowance. Where standard.md defines no routine slot (E Math), "routine" is not a verdict you may give. MARKS: when the marks do not match the work (standard.md, "marks match the work"), put a fix that starts with "re-mark" and says which part gains and which loses; that is a fix to the marks, not a reason to call the question below standard. WHOLE-PAPER items (standard.md marks them as the session's: the A Math List B, the E Math item 13) are not yours to enforce on one slot; never reject a slot for them. A question that re-skins ANY 2024/2025 question in standard-questions-P__P__.md (same situation or structure with new numbers) gets "too_close_to" = that ref.
3. Be exact about the key: work any disputed part yourself. A wrong key, a false "show that", or an unsolvable part means "all_agree": false and score ≤ 2.
4. VARIETY. Read `__RUN__/earlier-sets.md` once: every question our own earlier Sets (and this Set's other paper) have asked. The tutor: "the papers generated say set 1, set 2, set 3, .. should not be (too) similar to each other. should aim to test a wide variety of skills". Your brief's last section lists the earlier questions on this slot's topic and the nearest in wording. The same topic is expected. If this question shares a situation, a structure or a sequence of parts with one of them, or tests the same skill the same way, set "repeats_set" to that ref (the assembly step rejects the slot) and say in "fixes" which different skill of the topic to test. Check also `__RUN__/paper-so-far.md`: a skill already tested in THIS paper is a fix too. The setter's "skills" phrases must be true of the question and specific; if not, say so in "fixes".
5. Also read `__RUN__/Q<n>.gates.json`; if any gate failed, list it in "fixes".
AS GOOD AS SET 1. The tutor, same day: "just make sure the standard is as good as set 1 for am and em". Set 1 of this level is the paper the tutor read and approved; it is in `earlier-sets.md` in full. Put each slot beside the Set 1 question(s) of similar marks. If the slot is thinner, more scaffolded, more contrived (a strained context chosen only to be different) or less clean than Set 1, score it at most 3, set `"as_good_as_set1": false` and say in `fixes` what Set 1 does that this does not; otherwise set it to true. (When the run has no earlier Sets, omit the field.)

Reply with ONLY: the verdict file path(s), and one line per slot: "Q<n>: score S, all_agree A, standard: below/at/routine/above, too_close_to: …, repeats_set: …, as_good_as_set1: …, fixes: <count>".

You are the setter of slot Q__N__ of a new GCE O-Level E Math (4052) Paper __P__. The moderator did not accept your question. Read, in `__RUN__/`: `author-brief.md`, `standard.md`, `standard-questions-P__P__.md` (2024/25 difficulty anchors — not templates), `paper-so-far.md`, `Q__N__.brief.md`, your draft `Q__N__.json`, the gates result `Q__N__.gates.json`, the blind examiner's `Q__N__.blind.json` and the moderator's `Q__N__.verdict.json`.
Fix EVERY problem named (a wrong key → recompute and correct or restructure; a false "show that" → a true target; below the 2024/25 standard → remove the scaffold / hand the decision to the candidate / chain the ideas as the fixes say; a re-skin → a genuinely new situation and structure; a failed gate → what it names). If the question cannot be brought to standard by editing, write a NEW question for the slot instead. Work every part yourself; the numbers must be examination-clean. Overwrite `__RUN__/Q__N__.json` in exactly the author brief's JSON shape (backslashes doubled inside strings). Reply with ONLY the file path and one line per problem: "<problem> → <what changed>".

You are the setter of slot Q__N__ of a new GCE O-Level __SUBJECT__ (__CODE__) Paper __P__. The moderator did not accept your question. Read, in `__RUN__/`: `author-brief.md`, `standard.md`, `standard-questions-P__P__.md` (2024/25 difficulty anchors — not templates), `earlier-sets.md` (the variety rule — what our own earlier Sets have already asked), `paper-so-far.md`, `paper-shape.md` if it exists (the session's plan for the whole paper — if it names your slot, keep to it), `Q__N__.brief.md`, your draft `Q__N__.json`, the gates result `Q__N__.gates.json`, the blind examiner's `Q__N__.blind.json` and the moderator's `Q__N__.verdict.json`.
Fix EVERY problem named (a wrong key → recompute and correct or restructure; a false "show that" → a true target; below the 2024/25 standard → remove the scaffold / hand the decision to the candidate / chain the ideas as the fixes say; a re-skin → a genuinely new situation and structure; "repeats_set" or a too-close-to-our-own-Set gate → a DIFFERENT skill of the topic in a new situation, never the old question with new numbers; missing or vague `skills` → 1–3 specific phrases; a failed gate → what it names). If the question cannot be brought to standard by editing, write a NEW question for the slot instead. `"as_good_as_set1": false` → the tutor asked that every Set be as good as Set 1, the paper he read and approved (it is in `earlier-sets.md` in full): put your question beside the Set 1 question(s) of similar marks and rebuild what the verdict says is thinner, more scaffolded, more contrived or less clean; never fix it by moving closer to Set 1's content. Work every part yourself; the numbers must be examination-clean. Overwrite `__RUN__/Q__N__.json` in exactly the author brief's JSON shape (backslashes doubled inside strings). Reply with ONLY the file path and one line per problem: "<problem> → <what changed>".
